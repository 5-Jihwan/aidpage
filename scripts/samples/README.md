# scripts/samples — 활용신청 제출용 샘플

표준 라이브러리만 사용, 키는 `DATA_GO_KR_KEY` 환경변수. 운영 수집기는 `scripts/fetch_live.py`.

- `egen_sample.py` 응급의료 E-Gen: 실시간 가용병상·중증질환 수용·기관 메시지 (시도 시군구 인자)
- `kma_sample.py` 기상청: 초단기실황(nx ny) + 특보 목록
