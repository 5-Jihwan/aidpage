# 재난 타입 데이터 공백 → API 신청 목록 — 2026-09-04

> 근거: 재난 타입 어휘 스크리닝(artifact 882c999d) 매트릭스의 ◐(규칙 확정·데이터 대기)·✕(빠짐) 항목만 추렸다.
> 원칙: 이미 키가 있거나 파일로 받는 건 신청 안 한다. 시도 단위 자료는 타입 판정에 못 쓰고 맥락 변수로만 쓰므로 후순위.
> 활용목적 문안·IP는 docs/13 그대로.

## 0. 신청 없이 되는 것 (먼저 처리)

| 공백 | 왜 신청 불필요 | 할 일 |
|---|---|---|
| 위험개선지구 7유형 분리(물·산·바다·마름 정밀도) | safetydata DSSP-IF-00058 키 보유. 현재 집계가 flood 수를 세고 있으니 원자료에 유형 필드 있음 | 수집기 재실행 → `riskzone_by_sgg.json`을 7유형별 개수로 재집계(제 몫) |
| 불(산불) 타입 후보 | 산불발생이력 2013~ 이미 앱 레이어(SK safetydata) | '불' 채택 결정만 |
| 건조·대설·한파·폭염 특보 빈도 | 기상청 특보 API 사용 중(alerts 코퍼스) | 코퍼스 누적 기간 부족 → 과거분은 아래 SGIS·연보로 보강 |
| 급경사지 | safetydata 레이어 보유 | — |

## 1. 지금 신청할 것 (공공데이터포털, 자동승인 계열)

| 우선 | 데이터셋 | ID | 채우는 타입 | 단위 | 메모 |
|---|---|---|---|---|---|
| 1 | 지역별 하천 및 해안지역 인명피해 우려지역 | 15107562 | **바다**(강도), 물(하천) | 미확인(지역별) | 바다 강도 0 문제의 첫 해결책. 승인 후 첫 호출로 단위 확정 |
| 2 | 지역별 위험개선지구·소규모공공시설 기준 인명피해 우려지역 | 15107564 | 물·산 | 미확인(지역별) | 연도별 판(15107182)은 승인됨. 지역별 판이 시군구면 타입 변수, 시도면 맥락 |
| 3 | 특별재난지역 선포(자연재난) | 15107314 | 제도 렌즈(반복선포형), 물·바다 보강 | 지역별 | 위해 계열 직접 판정은 아니지만 "이 지역이 실제로 큰 피해를 반복했나"의 유일한 공식 기록 |
| 4 | 지역별 자연재난 원인별 피해 (재해연보 원인별: 호우·태풍·대설·강풍·지진·기타) | 포털에서 "원인별" 검색해 ID 확인 | **눈**(대설 피해 비중), **바다**(태풍 비중), **땅**(지진) | 시도 가능성 높음 | 시설 피해(15107320)는 지역별만이라 원인 구분이 없음. 원인별 판이 따로 있으면 신청, 없으면 재해연보 PDF 파싱 |

## 2. SGIS 인증키 1개로 한 번에 (sgis.kostat.go.kr, 무료·즉시)

| 메뉴 | 채우는 타입 | 메모 |
|---|---|---|
| 자연재해 통계지도 — 태풍별 영향범위 목록·통계정보 상세·년도별 태풍정보 | **바다** 강도(실영향범위 횟수) | 해안 접촉 근사 → 실제 태풍 영향 횟수로 교체 |
| 자연재해 통계지도 — 과거 폭염특보 목록·폭염 통계정보 상세 | **볕** | 특보 일수·영향 인구. 우리 코퍼스보다 긴 기간 |
| 자연재해 통계지도 — 홍수위험지도·산사태위험지도 영향범위 | 물·산 보조(위험지도=예측) | 이력 0인 103곳의 노출 판단. 이력과 다른 것임을 툴팁에 명시 |
| 센서스 가구·가구원·주택통계 | 취약(아동·주거) | 아동 연령 결정(0~4/0~9) 후 |

## 3. 심의 승인 대기·연계 신청

| 데이터셋 | ID | 채우는 타입 | 상태 |
|---|---|---|---|
| 공유플랫폼 인명피해우려지역(지구 단위, 7유형: 산사태·급경사지·하천·저지대침수·해안 너울·해안 해일·노후저수지) | 15139667 | 산·물·바다 전부(우산 레이어) | 사용자 심의승인 후 전달 예정. IP 제한(집·학교) 확인 |
| 산림청 산사태취약지역 | 산림청 산사태정보시스템 / 포털 "산사태취약지역" 검색 | **산**(법정 지정 근거 → '지정' 꼬리표) | 파일 제공인지 API인지 확인 후 신청. 격자 이력 없는 산지 24곳의 근거 보강 |

## 4. 후순위·신청 안 함

| 타입 | 이유 |
|---|---|
| 마름(가뭄) | 위험개선지구 '상습가뭄재해' 유형으로 §0에서 해결. 별도 가뭄 API 불필요 |
| 땅(지진) | 지진 피해 이력이 희소해 시도 단위 연보로 충분. 옥외대피장소는 보유 |
| 눈(한파 인명) | 한랭질환자 통계는 시도 단위 → 맥락 변수. 시군구 판정은 특보 코퍼스 누적으로 |
| 낙뢰·황사·강풍·화산·소행성 | 의도적 제외(장소 타입 부적합) |

## 5. 신청 순서 제안
1. SGIS 인증키(즉시) → 태풍·폭염 4종 수집기.
2. 포털 자동승인 3건(15107562·15107564·15107314) 한 번에 → 요청주소 복사해 전달.
3. 원인별 피해 데이터셋 존재 확인 → 있으면 신청.
4. 15139667 승인 도착 시 우산 레이어 + 서랍 재난 이력 연결.
